warning: line 17: this chain does not fuse -- 'sortBy' stops it, because it needs the whole collection, so it and every stage after it builds an array
warning: line 20: this chain does not fuse -- 'distinct' stops it, because it needs the whole collection, so it and every stage after it builds an array
warning: line 23: this chain does not fuse -- 'myOwnHelper' stops it, because xtpl cannot see inside it, so it and every stage after it builds an array
warning: line 49: 'fallback' turns off fusion for this chain -- a guard needs an expression and a fused loop is not one, so each stage builds an array again. Guard the part that can fail instead, and leave the chain outside it.
